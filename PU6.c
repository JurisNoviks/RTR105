#include <stdio.h>
#include <windows.h>

// Bez arg un bez return itkaaa
void pirmais()
{
    printf("Vnk teksts, bez funkcijas un argumentiem \n");
}

// ar agr un bez return
int main()
{
    int x, y, z;
    cin >> x >> y;
    z = x+y;
}

// Bez argumentiem, bet ar return
int main() {
  cout << "Hello World!";
  return 0;
}

// Ar argumentu un return
int
main (void)
{
  MessageBox (0, "Hello, World!",
              "Message", MB_OK);
  return 0;
}

// ar agr un ar return
int main()
{
    int x, y, z;
    cin >> x >> y;
    z = x+y;
    count << z<< endl;
    return 0;
}
